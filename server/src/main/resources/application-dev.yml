spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 注意：Spring Boot 通常不需要手动写 url，可以用 username/password/host 组合，
    # 但最稳妥的方式是直接写完整的 url
    url: jdbc:mysql://localhost:3306/sky_take_out?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
